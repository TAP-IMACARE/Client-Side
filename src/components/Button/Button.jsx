// eslint-disable-next-line no-unused-vars
import React from "react";
import "./Button.css";

// eslint-disable-next-line react/prop-types
const Button = ({ name }) => {
  return <button className="custom-BTN">{name}</button>;
};

export default Button;
